The rename serves the product's next life: hansard is being positioned as Second Foundation Labs' harness where agent work leaves a public record (forum-thread framing) instead of chat logs — the name is the first thing every new operator and the 2ndf.ai brand surface touch, and "trainlint" actively mis-sells it as an ML-training linter. Downstream consumer: every future operator onboarding via the marketplace + the 2ndf team (cz@2ndf.ai already an admin). Owner: voidrank.
