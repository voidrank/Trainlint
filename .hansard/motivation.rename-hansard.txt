"trainlint" is doubly outdated: the harness manages arbitrary research projects (not just training) and "lint" undersells a plan/execute/quiz/report/hosting product. Named hansard 2026-07-08 — the parliament's verbatim record of proceedings — after the operator's framing: a project becomes a forum thread ("把一个 project 弄成像论坛发帖一样"); work leaves a public record, not a chat log. Naming diligence trail lives in memory rename-trainlint-to-hansard.
