Rename the product trainlint → hansard everywhere a HUMAN sees the name — plugin id + /hansard:* command prefix via repo/marketplace rename [plugin-id-migration], brand strings, HANSARD_* env vars with TRAINLINT_* fallback — while every piece of STATE survives untouched (data_root substrate, R2 bucket objects, upload tokens, browser cookies, viewers' localStorage annotations) [state-continuity], and the concurrently-active dev session (0.3.56 and moving) is never broken mid-flight [rename-boundary decides what is permanently grandfathered]. DONE = /hansard:plan works after reinstall; old TRAINLINT_ env and existing browser sessions still work; zero substrate/report/annotation loss; the other session lands its work without conflict.
