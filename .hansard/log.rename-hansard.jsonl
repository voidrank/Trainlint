# durable append-only annotation log (harvested from sessions). starts empty.
{"ts":"2026-07-08","kind":"decide","direction":"rename-boundary","note":"Operator set the boundary: rename only human-visible surfaces (/hansard:* + repo + brand + HANSARD_* env w/ fallback); permanently grandfather all 5 state-carrying ids (R2 bucket, cookie, localStorage, data dirname, worker name). Cascaded: state-continuity, data-root-migration, plugin-id-migration all decided; main thread moves to concurrent-session-handoff."}
{"ts":"2026-07-08","kind":"verify","direction":"brand-strings","note":"Portal rebranded Hansard live on secondfoundationlabs.com (commit 3b64ed4, worker version 572f6ae1@100%): Login-hansard title, Hansard card, tagline The work on the record — zero path/cookie changes, probes confirm."}
{"ts":"2026-07-09","kind":"verify","direction":"state-continuity","note":"Old trainlint_token cookie authenticates 200 against the renamed worker (dual-read verified on the local deployment); anon-suffix endsWith checks widened to match both eras (b66946e)."}
{"ts":"2026-07-09","kind":"build","direction":"plugin-id-migration","note":"Local worker deployment (report-server/local.sh, 0887fe1) caught the rename commit's getSessCookie infinite recursion on its FIRST run — before the code ever reached Cloudflare. Fixed in b66946e; bisect table: 3b64ed4 clean / 263149d 500s."}
