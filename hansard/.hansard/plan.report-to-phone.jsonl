# Project PLAN: the ordered DECISIONS that define this run, each tagged with the
# transferable PRINCIPLE that governs it. Draft it with /trainlint:plan. See plan.example.jsonl.
# fields: id | phase | decision | choice | principle | why | status(open|decided|verified) | match(regex)
{"id": "send-html-not-png", "phase": "deliver", "pillar": true, "decision": "What does the close SendUserFile to put the report on the phone?", "choice": "The self-contained viz report HTML itself, with display:'render' -- the Claude mobile app renders HTML inline (SendUserFile contract), so the phone gets the FULL report. NOT a PIL-rendered PNG card. [operator-directed] VERIFIED 2026-07-02 by the on-phone render test.", "principle": "send-the-artifact-not-a-picture-of-it", "why": "display:'render' opens HTML/SVG/PDF inline in the app; the earlier 'an HTML path is useless on a phone' premise confused sending a PATH (text) with sending the FILE (rendered). The PNG card was a workaround for a non-problem, and it only showed a glance, not the report.", "status": "verified", "match": "SendUserFile|display.*render|mobile\\.png|deliver", "not_this": "generating a PNG/image card as the phone deliverable", "not_re": "_render_png|mobile\\.png|PIL.*card|Image\\.new", "artifact": "/home/shiyil/Trainlint/trainlint/hooks/reportcheck.py"}
{"id": "render-in-app-verifies", "phase": "verify", "load_bearing": true, "decision": "Does display:'render' on the JS-heavy viz report HTML actually render acceptably inline in the phone Claude app (vs stripped-JS / blank / a download link)?", "choice": " VERIFIED 2026-07-02: sent the 53KB JS-heavy session-project-lock.html with display:'render'; operator confirms it renders normally as a readable report inline on the phone Claude app. Approach stands.", "principle": "verify-the-render-target", "why": "the viz report HTML carries browser-side JS (per-decision chatbots, deck JS, localStorage). If the app's inline render strips JS or shows blank, 'just send the existing HTML' fails and we'd send the lighter slides deck or a static export instead. Cheapest test that could change the shape: SendUserFile a real report HTML with display:render and look at the phone.", "status": "verified", "match": "render|display|inline|\\.html|verify"}
{"id": "which-html", "phase": "deliver", "pillar": true, "decision": "Which HTML travels to the phone -- the interactive report, the slides deck, or both?", "choice": " DECIDED (operator 2026-07-02: ship BOTH): the interactive report <name>.html AND the slides deck <name>.slides.html both travel to the phone -- the close SendUserFile's each with display:'render'. Report = full detail + per-decision chatbots; deck = glanceable paged view. BUILT: viz.py PHONE sign-off names BOTH files; command docs instruct sending both; the delivery gate now requires both html sends (test_reportcheck 14/14). RESIDUAL: render-in-app-verifies confirmed the REPORT renders inline on the phone; the slides deck is the same self-contained JS-heavy HTML so it should render alike, but an on-phone slides render is not yet separately confirmed -> follow-up before marking verified. VERIFIED 2026-07-02 (on-phone): both views SendUserFile'd with display:'render' in a remote-control session; operator confirmed the slides deck renders inline as a clean paged deck (the report was already confirmed by render-in-app-verifies). Both views proven on-phone -> the residual is closed.", "principle": "match-the-phone-read", "why": "the interactive report (<name>.html) is full detail + the Ask-about-this chatbots; the slides deck (<name>.slides.html) is a clean, paged, glanceable version that likely renders lighter inline. Which one is the phone deliverable (or both) depends on how each renders in the app -- gated by render-in-app-verifies. UPDATE 2026-07-02: operator chose BOTH over report-only -- the deck is a lighter glanceable companion to the full report, so both go to the phone.", "status": "verified", "match": "slides|report|\\.html|which", "artifact": "/home/shiyil/Trainlint/trainlint/research/viz.py", "scope_add": ["the slides deck also travels to the phone"]}
{"id": "drop-the-png", "phase": "deliver", "decision": "What happens to the PIL PNG phone-card (mobile.py)?", "choice": "REMOVE it -- drop mobile.py's PNG generation, the viz.generate() mobile build, and the `MOBILE: <png>` print line. The report HTML replaces it entirely. [operator: not PNG] DONE 2026-07-02: deleted research/mobile.py; viz.generate() returns (html, slides, d) only; the MOBILE: png line is gone.", "principle": "delete-the-workaround", "why": "once the HTML renders inline, the glance-card PNG is dead weight; keeping it invites drift back to sending an image.", "status": "verified", "artifact": "", "match": "mobile\\.py|_render_png|mobilepath|MOBILE:"}
{"id": "delivery-gate-rework", "phase": "gate", "pillar": true, "decision": "How does reportcheck's delivery gate enforce the new HTML send?", "choice": "Rework E2 + _sent_report_html to require the close to SendUserFile BOTH the report .html AND the slides .html (display render), naming whichever view is missing; keep the one-bounce, fail-open, report-shaped-only behavior. UPDATED 2026-07-02 (both-travel): _sent_report_html returns (report, slides) -- .slides.html stripped before the report leg so a slides-only send doesn't satisfy the report; E2 requires both. test_reportcheck 14/14 (no-send->bounce, report-only->slides-missing, slides-only->report-missing, both->clear).", "principle": "the-gate-follows-the-contract", "why": "the gate currently matches SendUserFile inputs containing 'mobile'/'.png' (reportcheck.py:118). After the switch that would never pass; it must key on the HTML send instead, or it bounces every correct close.", "status": "verified", "artifact": "/home/shiyil/Trainlint/trainlint/hooks/reportcheck.py", "match": "reportcheck|_sent_mobile|DELIVERY|SendUserFile"}
{"id": "command-instructions", "phase": "gate", "decision": "What do plan.md / execute-and-report.md tell the agent to send?", "choice": "SendUserFile BOTH the report <name>.html AND the slides deck <name>.slides.html with display:'render' at the close; keep surfacing HTML:/SLIDES: paths for the laptop. UPDATED 2026-07-02 (both-travel): plan.md + execute-and-report.md now instruct sending BOTH html files (was: the report html only).", "principle": "the-doc-follows-the-contract", "why": "the close instructions currently tell the agent to send the .png with a rationale that's now wrong; they must direct the HTML render-send or the agent keeps sending the PNG.", "status": "verified", "artifact": "/home/shiyil/Trainlint/trainlint/commands/plan.md", "match": "plan\\.md|execute-and-report|SendUserFile|display"}
