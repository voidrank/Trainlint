Deliver the trainlint report to the phone by SendUserFile-ing the self-contained HTML report with display:'render' (which the Claude mobile app renders inline) [send-html-not-png], REPLACING the PIL PNG phone-card -- so every plan/execute close puts the FULL interactive report on the phone, not a glance image. The other pillars: which HTML travels (interactive report vs slides deck) [which-html], and the delivery gate that enforces the HTML send rather than a .png [delivery-gate-rework].
Bar for done: every plan/execute close SendUserFile's the report HTML with display:render; reportcheck's delivery gate enforces the HTML send (not a .png); the PNG card generation (mobile.py) is removed; and it is verified the report HTML renders inline in the phone Claude app.
