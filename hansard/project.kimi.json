{
  "_comment": "PROJECT FACTS for the 'kimi' project = porting hansard to the Kimi CLI. This is a TOOLING project, not a training run, so the doorman's training-danger patterns don't apply; the kept keys are deliberately narrow so the action-doorman stays quiet here. The real artifact is the decision spine in research/plan.kimi.jsonl.",
  "reference_impl": "the Claude Code + Codex ports (hooks/codex_compat.py, codex/hooks.json)",
  "frozen_component": "the Kimi hook protocol (kimi_cli/hooks/*, read at v1.48.0)",
  "codec_contract": {
    "config_dir_env": "KIMI_SHARE_DIR (else ~/.kimi)",
    "hook_config": "config.toml: hooks = [{event, command, matcher, timeout}]",
    "block_signals": "exit 2, or stdout permissionDecision:deny",
    "tools": "Shell | WriteFile | StrReplaceFile | ReadFile | Grep | Glob"
  }
}
