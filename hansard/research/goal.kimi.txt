Port trainlint to the Kimi CLI (Kimi Code, a Python coding agent; hooks live in ~/.kimi/config.toml) so its SAFETY GATE runs unchanged. Bar for "done": on a real Kimi session, a dangerous training action is REJECTED (PreToolUse -> permissionDecision:deny), a bad plan-report is BOUNCED for rewrite (Stop -> block+reason), and harvest fires (PreCompact/SessionEnd) — with Kimi's block-only-hook constraint (no additionalContext/systemMessage injection) explicitly handled for the soft channels (compass/coach/escalate), not silently dropped. The three pillars: the hook-protocol contract · the block-only output model · the safety-gate (reject) channel.
